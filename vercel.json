{
  "buildCommand": "npm run data && npm run og && npm run build",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "trailingSlash": false,
  "rewrites": [
    { "source": "/name/:slug", "destination": "/name/lookup" }
  ]
}
